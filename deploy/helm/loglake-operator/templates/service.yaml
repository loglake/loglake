{{- if .Values.metricsService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "loglake-operator.fullname" . }}
  labels:
    {{- include "loglake-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "loglake-operator.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.metricsService.port }}
      targetPort: metrics
      protocol: TCP
{{- end }}
