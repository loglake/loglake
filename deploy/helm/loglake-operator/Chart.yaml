apiVersion: v2
name: loglake-operator
description: |
  Kubernetes operator for loglake: reconciles LoglakeCluster CRs into
  ingester / compactor / query-server Deployments and drives
  per-tenant lifecycle + autoscaling.
type: application
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/limnion-ai/loglake
sources:
  - https://github.com/limnion-ai/loglake
keywords:
  - loglake
  - operator
  - logs
  - observability
