apiVersion: v2
name: loglake-operator
description: |
  Kubernetes operator for loglake: reconciles LoglakeCluster CRs into
  ingester / compactor / query-server Deployments and drives
  per-tenant lifecycle + autoscaling.
type: application
version: 0.2.0
appVersion: "0.2.0"
home: https://github.com/loglake/loglake
sources:
  - https://github.com/loglake/loglake
keywords:
  - loglake
  - operator
  - logs
  - observability
