{
 "uid": "loglake-overview",
 "title": "loglake overview",
 "tags": [
  "loglake",
  "logs",
  "iceberg"
 ],
 "timezone": "browser",
 "schemaVersion": 38,
 "version": 2,
 "refresh": "30s",
 "time": {
  "from": "now-30m",
  "to": "now"
 },
 "templating": {
  "list": [
   {
    "name": "datasource",
    "type": "datasource",
    "query": "prometheus",
    "current": {
     "selected": false,
     "text": "Prometheus",
     "value": "Prometheus"
    },
    "hide": 0
   },
   {
    "name": "namespace",
    "type": "query",
    "datasource": {
     "type": "prometheus",
     "uid": "${datasource}"
    },
    "query": "label_values(loglake_ingest_requests_total, namespace)",
    "refresh": 2,
    "includeAll": true,
    "allValue": ".*",
    "current": {
     "text": "All",
     "value": "$__all"
    }
   }
  ]
 },
 "panels": [
  {
   "id": 133,
   "type": "stat",
   "title": "Build provenance",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 4,
    "w": 24,
    "x": 0,
    "y": 0
   },
   "targets": [
    {
     "expr": "max by (pod, version, commit) (loglake_build_info{namespace=~\"$namespace\"})",
     "instant": true,
     "legendFormat": "{{pod}} · {{version}} ({{commit}})",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "decimals": 0
    }
   },
   "options": {
    "reduceOptions": {
     "values": false,
     "calcs": [
      "lastNotNull"
     ],
     "fields": ""
    },
    "orientation": "horizontal",
    "textMode": "name",
    "colorMode": "none",
    "graphMode": "none",
    "justifyMode": "auto"
   },
   "description": "Build version and commit reported by each scraped loglake server process."
  },
  {
   "id": 100,
   "type": "row",
   "title": "Ingest",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 4
   },
   "collapsed": false
  },
  {
   "id": 101,
   "type": "timeseries",
   "title": "Events accepted (per-sec)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 0,
    "y": 5
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_events_accepted_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "EPS",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "rps"
    }
   }
  },
  {
   "id": 102,
   "type": "timeseries",
   "title": "Ingest requests by status",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 6,
    "y": 5
   },
   "targets": [
    {
     "expr": "sum by (status) (rate(loglake_ingest_requests_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "{{status}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "rps"
    }
   }
  },
  {
   "id": 103,
   "type": "timeseries",
   "title": "Ingest request p99 latency",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 12,
    "y": 5
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.99, sum by (le, endpoint) (rate(loglake_ingest_request_duration_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "{{endpoint}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   }
  },
  {
   "id": 104,
   "type": "timeseries",
   "title": "WAL throughput (bytes + seals)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 18,
    "y": 5
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_wal_bytes_written_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "bytes/s",
     "refId": "A"
    },
    {
     "expr": "sum(rate(loglake_wal_segments_sealed_total{namespace=~\"$namespace\"}[1m])) * 60",
     "legendFormat": "seals/min",
     "refId": "B"
    }
   ]
  },
  {
   "id": 105,
   "type": "row",
   "title": "Durability (silent-loss signals — these should sit at zero)",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 13
   },
   "collapsed": false
  },
  {
   "id": 106,
   "type": "timeseries",
   "title": "Mirror registrations abandoned",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 5,
    "x": 0,
    "y": 14
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_wal_mirror_register_abandoned_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "abandoned",
     "refId": "A"
    }
   ],
   "description": "Rows acked and uploaded, with no catalog row: durable and permanently unqueryable. The 2026-08-15 incident. Any non-zero value is data loss."
  },
  {
   "id": 107,
   "type": "timeseries",
   "title": "Mirror uploads abandoned / failing",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 5,
    "x": 5,
    "y": 14
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_wal_mirror_upload_abandoned_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "abandoned",
     "refId": "A"
    },
    {
     "expr": "sum by (reason) (rate(loglake_wal_mirror_failures_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "fail {{reason}}",
     "refId": "B"
    }
   ],
   "description": "Abandoned means the PVC is now the only copy of those rows."
  },
  {
   "id": 173,
   "type": "timeseries",
   "title": "WAL mirror pin duration",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 5,
    "x": 10,
    "y": 14
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_wal_mirror_pin_duration_seconds_sum{namespace=~\"$namespace\"}[5m])) / sum(rate(loglake_wal_mirror_pin_duration_seconds_count{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "average",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_wal_mirror_pin_duration_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   },
   "description": "Synchronous seal time for the whole mirror pin operation, including local lookup, hard-link creation and the mirror-pending directory fsync. The metric does not attribute those components separately."
  },
  {
   "id": 108,
   "type": "timeseries",
   "title": "WAL recovery / integrity",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 4,
    "x": 15,
    "y": 14
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_wal_crc_mismatch_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "crc mismatch",
     "refId": "A"
    },
    {
     "expr": "sum(increase(loglake_wal_partials_adopted_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "partials adopted",
     "refId": "B"
    },
    {
     "expr": "sum(increase(loglake_wal_partial_tail_dropped_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "partial tails dropped",
     "refId": "C"
    }
   ],
   "description": "Adoption is expected after a pod dies mid-write. A dropped tail means the recovered partial ended during an IPC message; its complete fsynced prefix remains readable and the original bytes remain on disk."
  },
  {
   "id": 109,
   "type": "timeseries",
   "title": "Schema drift",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 5,
    "x": 19,
    "y": 14
   },
   "targets": [
    {
     "expr": "sum by (disposition) (increase(loglake_storage_schema_drift_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{disposition}}",
     "refId": "A"
    }
   ],
   "description": "disposition=refused means writes are being rejected because a table is behind the running binary. Run migrate-schema --all-tables --all-namespaces."
  },
  {
   "id": 127,
   "type": "timeseries",
   "title": "Group-count delta writes lost / retried",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 22
   },
   "targets": [
    {
     "expr": "sum by (iceberg_namespace, table) (increase(loglake_group_count_delta_write_failures_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "lost {{iceberg_namespace}}.{{table}}",
     "refId": "A"
    },
    {
     "expr": "sum by (iceberg_namespace, table) (increase(loglake_group_count_delta_write_retries_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "retried {{iceberg_namespace}}.{{table}}",
     "refId": "B"
    },
    {
     "expr": "sum by (iceberg_namespace, table) (increase(loglake_side_aggregate_publish_failures_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "side publication lost {{iceberg_namespace}}.{{table}}",
     "refId": "C"
    }
   ],
   "description": "All three series are grouped by Iceberg namespace and table: one commit path writes for the base namespace and every tenant namespace, each with its own events table. A lost delta leaves the aggregate short, but writes a durable marker that the maintenance compactor normally repairs on its next aggregate fold; a sustained retry rate means the object store or credentials are flaky and a loss is likely. The third series is the inline side object's own publication: it retries on the same budget, and a loss there leaves that object short of total-records with only its group counts repairable."
  },
  {
   "id": 143,
   "type": "timeseries",
   "title": "Group-count automatic rebuilds",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 22
   },
   "targets": [
    {
     "expr": "sum by (iceberg_namespace, table, outcome) (increase(loglake_group_count_auto_rebuilds_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{outcome}} {{iceberg_namespace}}.{{table}}",
     "refId": "A"
    }
   ],
   "description": "success means the compactor restored the aggregate from committed files after a lost delta. incomplete or failed means GROUP BY remains on the exact per-file path and fires LoglakeGroupCountDeltaLost; use loglake rebuild-group-counts --namespace <iceberg_namespace> --table <table> as the operator fallback."
  },
  {
   "id": 110,
   "type": "row",
   "title": "Drain and compaction",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 30
   },
   "collapsed": false
  },
  {
   "id": 111,
   "type": "timeseries",
   "title": "Drain backlog (segments + bytes)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 0,
    "y": 31
   },
   "targets": [
    {
     "expr": "sum by (namespace) (max by (namespace, tenant) (loglake_compactor_sealed_pending{namespace=~\"$namespace\"}))",
     "legendFormat": "{{namespace}} segments",
     "refId": "A"
    },
    {
     "expr": "sum by (namespace) (max by (namespace, tenant) (loglake_compactor_sealed_pending_bytes{namespace=~\"$namespace\"}))",
     "legendFormat": "{{namespace}} bytes",
     "refId": "B"
    }
   ],
   "description": "The real queue depth, from peek_pending. Read WITH the oldest-age panel: a deep queue draining fast is healthy. Under the catalog claim peek_pending counts the whole sealed table with no worker filter, so every compactor replica publishes the same total: the inner max takes each tenant's queue once however many workers report it, and a bare sum would multiply the backlog by the replica count. One line per namespace, so two clusters keep two totals."
  },
  {
   "id": 112,
   "type": "timeseries",
   "title": "Oldest unclaimed segment age",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 6,
    "y": 31
   },
   "targets": [
    {
     "expr": "max by (namespace) (loglake_compactor_sealed_pending_oldest_age_seconds{namespace=~\"$namespace\"})",
     "legendFormat": "{{namespace}} oldest age",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   },
   "description": "The growth signal a depth count cannot give. Steady depth with a rising age is a drain that is not keeping up. Under the catalog claim every compactor replica reports the same oldest age, and the max takes it once. One line per namespace, matching the depth panel, so a healthy cluster's age is not hidden behind a worse one's."
  },
  {
   "id": 113,
   "type": "timeseries",
   "title": "Rows + segments committed",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 12,
    "y": 31
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_compactor_rows_committed_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "rows/s",
     "refId": "A"
    },
    {
     "expr": "sum(rate(loglake_compactor_segments_committed_total{namespace=~\"$namespace\"}[5m])) * 60",
     "legendFormat": "segments/min",
     "refId": "B"
    },
    {
     "expr": "sum(rate(loglake_compactor_segments_swept_total{namespace=~\"$namespace\"}[5m])) * 60",
     "legendFormat": "swept/min",
     "refId": "C"
    }
   ]
  },
  {
   "id": 114,
   "type": "timeseries",
   "title": "Cycles by outcome",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 18,
    "y": 31
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_compactor_cycles_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "{{outcome}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "rps"
    }
   }
  },
  {
   "id": 115,
   "type": "timeseries",
   "title": "Watchdog trips",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 4,
    "x": 0,
    "y": 39
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_compactor_watchdog_trips_total{namespace=~\"$namespace\"}[15m]))",
     "legendFormat": "trips",
     "refId": "A"
    }
   ],
   "description": "Compaction being killed by its own deadline. Tripped 52 times on 2026-08-15 with nothing watching it."
  },
  {
   "id": 116,
   "type": "timeseries",
   "title": "Segments quarantined",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 4,
    "y": 39
   },
   "targets": [
    {
     "expr": "sum(loglake_catalog_claim_quarantined{namespace=~\"$namespace\"})",
     "legendFormat": "claim quarantine",
     "refId": "A"
    },
    {
     "expr": "sum(loglake_compactor_segments_poisoned{namespace=~\"$namespace\"})",
     "legendFormat": "poison/ set-aside",
     "refId": "B"
    },
    {
     "expr": "sum by (namespace, tenant) (loglake_compactor_orphans_held{namespace=~\"$namespace\"})",
     "legendFormat": "orphans/ held {{namespace}}/{{tenant}}",
     "refId": "C"
    }
   ],
   "description": "Set aside and not draining without intervention: repeated drain failures (usually an index that does not exist), or a mirrored segment refused because it cannot be placed in the table its index resolves to now, which is set aside on the first cycle. Durable and unqueryable until requeued. The second line is the local filesystem drain's equivalent (#3143): a segment that failed to read on every attempt is held under `<wal>/poison/` with a note saying why, and `loglake wal-requeue --wal <wal-root>` is the way back once its cause is fixed. `loglake_compactor_segments_poisoned_total` is the event arm — a set-aside that is requeued and set aside again counts twice there and once here. The third line is what `LoglakeCompactorOrphansHeld` pages on (#3267): quarantined segments under `<wal>/orphans/` that automatic disposition could not settle, because the names are absent from the table's consumed set and the snapshot that would prove whether they were committed is no longer retained. Their commit status is UNKNOWN and the files must be preserved — the rows may already be in the table or may exist nowhere else, so deleting one can lose rows and requeueing one can duplicate them, and either needs evidence from your own retention and ingest history first. Broken out by tenant, unlike the two summed lines, because the hold is per tenant and the answer to 'whose?' is the first thing the page needs; a rising line is a hold that is still growing. It is a filesystem-drain observation only: the catalog-claim drain publishes no reading at all, so no line here does not establish that a former WAL PVC has no orphans left under it."
  },
  {
   "id": 117,
   "type": "timeseries",
   "title": "Reclaims without proof",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 4,
    "x": 10,
    "y": 39
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_compactor_reclaim_unprovable_total{namespace=~\"$namespace\"}[30m]))",
     "legendFormat": "unprovable",
     "refId": "A"
    }
   ],
   "description": "The snapshots proving whether a segment was already committed had expired, so requeueing it may duplicate rows. Widen snapshot retention."
  },
  {
   "id": 171,
   "type": "timeseries",
   "title": "Orphan dispositions",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 14,
    "y": 39
   },
   "targets": [
    {
     "expr": "sum by (action) (increase(loglake_compactor_orphans_disposed_total{namespace=~\"$namespace\"}[30m]))",
     "legendFormat": "{{action}} / 30m",
     "refId": "A"
    },
    {
     "expr": "sum by (action) (loglake_compactor_orphans_disposed_total{namespace=~\"$namespace\"})",
     "legendFormat": "{{action}} (cumulative)",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Filesystem-drain-only dispositions of quarantined files under `<wal>/orphans/`. deleted means proven committed and removed; requeued means proven uncommitted and returned to sealed/ for draining. The 30-minute increase shows recent decisions. The cumulative companion preserves first-event visibility because each action and tenant series is registered only on its first positive disposition; absence or a flat increase does not prove that no files were settled. The catalog-claim drain does not visit WAL orphan directories or publish this counter."
  },
  {
   "id": 118,
   "type": "timeseries",
   "title": "Commit p99 latency",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 4,
    "x": 20,
    "y": 39
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_compactor_commit_duration_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   }
  },
  {
   "id": 134,
   "type": "timeseries",
   "title": "Mirror sync duration (avg / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 0,
    "y": 47
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_compactor_mirror_sync_duration_seconds_sum{namespace=~\"$namespace\"}[15m])) / sum(rate(loglake_compactor_mirror_sync_duration_seconds_count{namespace=~\"$namespace\"}[15m]))",
     "legendFormat": "average",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_compactor_mirror_sync_duration_seconds_bucket{namespace=~\"$namespace\"}[15m])))",
     "legendFormat": "p99",
     "refId": "B"
    },
    {
     "expr": "sum(loglake_compactor_mirror_sync_rotation_duration_seconds_sum{namespace=~\"$namespace\"}) / sum(loglake_compactor_mirror_sync_rotation_duration_seconds_count{namespace=~\"$namespace\"})",
     "legendFormat": "completed rotation avg",
     "refId": "C"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   },
   "description": "Average wall time per completed or failed bounded page and the fleet-wide page p99 over 15m. Completed rotation avg is the whole durable wraparound scan, including cadence gaps and owner handoff, averaged since the current pods started. Read with page and rotation object counts to separate storage/catalog latency from retained-prefix growth."
  },
  {
   "id": 135,
   "type": "timeseries",
   "title": "Mirror sync objects per pass",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 8,
    "y": 47
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_compactor_mirror_sync_objects_sum{namespace=~\"$namespace\"}[15m])) / sum(rate(loglake_compactor_mirror_sync_objects_count{namespace=~\"$namespace\"}[15m]))",
     "legendFormat": "objects/page avg",
     "refId": "A"
    },
    {
     "expr": "sum(loglake_compactor_mirror_sync_rotation_objects_sum{namespace=~\"$namespace\"}) / sum(loglake_compactor_mirror_sync_rotation_objects_count{namespace=~\"$namespace\"})",
     "legendFormat": "retained/rotation avg",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    }
   },
   "description": "Objects/page preserves the bounded-pass meaning of loglake_compactor_mirror_sync_objects and should plateau at 1,024 while a rotation is in progress. Retained/rotation is the sealed .arrow population examined by each completed wraparound rotation, averaged since the current pods started; it can move slightly during a rotation because retention runs between pages. A rising retained count with proportional rotation duration means retention backlog, while a flat page count alone does not prove recovery is converging."
  },
  {
   "id": 136,
   "type": "timeseries",
   "title": "Mirror repairs / rotation completion",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 16,
    "y": 47
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_compactor_mirror_sync_registered_total{namespace=~\"$namespace\"}[15m])) * 60",
     "legendFormat": "registered/min",
     "refId": "A"
    },
    {
     "expr": "time() - max(loglake_compactor_mirror_sync_last_completed_timestamp_seconds{namespace=~\"$namespace\"})",
     "legendFormat": "completion age",
     "refId": "B"
    },
    {
     "expr": "max(loglake_compactor_mirror_sync_rotations_completed{namespace=~\"$namespace\"})",
     "legendFormat": "rotations completed",
     "refId": "C"
    },
    {
     "expr": "loglake_compactor_mirror_sync_rotation_objects_examined{namespace=~\"$namespace\"} and on(namespace, pod) increase(loglake_compactor_mirror_sync_total{namespace=~\"$namespace\"}[5m]) > 0",
     "legendFormat": "examined {{pod}}",
     "refId": "F"
    },
    {
     "expr": "sum(rate(loglake_compactor_mirror_sync_total{namespace=~\"$namespace\"}[15m])) * 60",
     "legendFormat": "pages/min",
     "refId": "D"
    },
    {
     "expr": "sum(rate(loglake_compactor_mirror_sync_rotation_objects_count{namespace=~\"$namespace\"}[6h])) * 3600",
     "legendFormat": "rotations completed/h",
     "refId": "E"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops"
    },
    "overrides": [
     {
      "matcher": {
       "id": "byFrameRefID",
       "options": "B"
      },
      "properties": [
       {
        "id": "unit",
        "value": "s"
       },
       {
        "id": "custom.axisPlacement",
        "value": "right"
       }
      ]
     }
    ]
   },
   "description": "Registered/min counts catalog rows actually inserted by reconciliation, excluding idempotent conflicts. Completion age is seconds since the durable cursor last wrapped; it survives owner handoff and is the backlog/stall reader when every bounded page remains full. Examined by pod is durable examined-to-date progress, published after every successful page; it rises within a rotation and resets to the first page's count after a wrap, so it is not a remaining-work gauge. The five-minute page-activity filter hides the retained gauge of a live former owner after lease handoff. Rotations completed is the durable cursor generation, not a per-pod event counter. The two stall readers LoglakeMirrorReconciliationStalled ANDs are pages/min (bounded reconciliation passes are running at all — 0 means reconciliation is off, unelected, or the compactor is gone, none of which that alert pages for) and rotations completed/h, which is the wraparound rate the alert requires to be nonzero over prometheusRule.mirrorRotationStallSecs. Pages/min above zero with rotations completed/h pinned at zero is the stall: the prefix is being walked and never finished."
  },
  {
   "id": 155,
   "type": "timeseries",
   "title": "Delete tasks left non-terminal (last observation)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 55
   },
   "targets": [
    {
     "expr": "sum by (state) (loglake_compactor_delete_tasks_nonterminal{namespace=~\"$namespace\"})",
     "legendFormat": "non-terminal {{state}}",
     "refId": "A"
    },
    {
     "expr": "sum by (state) (increase(loglake_compactor_delete_tasks_stalled_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "stalled observations/h {{state}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "LAST OBSERVATION, not live state. The compactor's delete sweep publishes these counts once per cycle, before it executes anything, and only when it could see every non-terminal task in every namespace; a live pod therefore RETAINS the last full number when delete-task execution is switched off, when it loses the delete_tasks lease, when the stage's watchdog trips or when a listing fails. That is why the alert (LoglakeDeleteTaskStalled) reads the stalled counter and not this gauge, and why check-chart.py refuses a rule that reads it. running is a task claimed and started that never reached done or failed; pending_claimed is one claimed by an executor that died before writing running. Both are aged from the claim OBJECT, which bounds execution duration from above and keeps growing after the task strands — it is not a running duration, and a stalled observation proves neither that the executor is gone nor whether its rewrite committed. Non-terminal counts above zero are normal while a delete task is executing; the stalled arm is what says one has outlived every execution window this compactor allows. Recovery is an explicit resubmission under a new task id."
  },
  {
   "id": 156,
   "type": "timeseries",
   "title": "WAL owner mismatches (dropped incarnation held back)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 55
   },
   "targets": [
    {
     "expr": "sum by (tenant, index) (increase(loglake_compactor_wal_owner_mismatch_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "owner mismatch/h {{tenant}} {{index}}",
     "refId": "A"
    },
    {
     "expr": "sum by (tenant, index) (increase(loglake_compactor_wal_stale_segments_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "segment refusals/h {{tenant}} {{index}}",
     "refId": "B"
    },
    {
     "expr": "sum(loglake_compactor_wal_owner_mismatch_total{namespace=~\"$namespace\"})",
     "legendFormat": "owner mismatch (cumulative)",
     "refId": "C"
    },
    {
     "expr": "sum(loglake_compactor_wal_stale_segments_total{namespace=~\"$namespace\"})",
     "legendFormat": "segment refusals (cumulative)",
     "refId": "D"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Both counters count refusal EVENTS, which is not a count of distinct objects held back: a mirrored object requeued out of quarantine and refused again counts again, and a filesystem segment whose move under stale/ fails stays in sealed/ and is counted on every cycle that re-examines it. Owner mismatch is the prefix-level finding — a WAL directory or a mirror key prefix stamped for a dropped incarnation of the index. On the filesystem it counts the segments that move under stale/; on the mirror there is nothing to move, so it counts the re-stamp, once. Segment refusals are per object: a frame header naming a table the index no longer resolves to, or (under a prefix that has itself named a dropped table) an object carrying no identity at all. Filesystem segments go under stale/; mirrored objects stay where they are and their claim is quarantined on the cycle that refuses it, since no retry can change a verdict a never-reused table uuid makes terminal — which Segments quarantined and LoglakeSegmentsQuarantined already read. The cumulative arms are here because both series are labelled by tenant and appear only with their first increment, so a single filesystem event has no rate to read but does move the total. Above zero with the drain otherwise healthy is the narrow case #2729 left behind: the index commits normally while individual objects are held back, and requeue_quarantined is the deliberate way back."
  },
  {
   "id": 163,
   "type": "timeseries",
   "title": "Mirror objects left unreclaimed (ledger mark never landed)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 63
   },
   "targets": [
    {
     "expr": "sum(increase(loglake_compactor_mirror_unreclaimed_total{namespace=~\"$namespace\"}[30m]))",
     "legendFormat": "unreclaimed",
     "refId": "A"
    },
    {
     "expr": "sum(increase(loglake_compactor_mirror_mark_errors_total{namespace=~\"$namespace\"}[30m]))",
     "legendFormat": "mark errors",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Both are zero unless compactor.mirrorLedgerReclaim is on, and both should stay zero when it is. The filesystem drain marks its locally-committed segments in the wal_segments ledger, and committed retention deletes the mirror object and then the row. Unreclaimed counts the other outcome: the mark never became durable, so the local committed/ file was swept at the 3600-second ceiling to keep the WAL volume bounded, and that object is now beyond this drain's reach -- it needs an object-store lifecycle rule or a manual pass. Mark errors are the cause to read first; a Postgres outage lasting longer than the ceiling produces both."
  },
  {
   "id": 119,
   "type": "row",
   "title": "Layout (query latency tracks depth)",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 71
   },
   "collapsed": false
  },
  {
   "id": 120,
   "type": "timeseries",
   "title": "Overlap depth",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 72
   },
   "targets": [
    {
     "expr": "max by (table) (loglake_table_overlap_depth{namespace=~\"$namespace\"})",
     "legendFormat": "{{table}}",
     "refId": "A"
    }
   ],
   "description": "Scanning queries degrade with depth: the 2026-08-27 round measured a windowed browse at 40.9s against 0.13s converged. Target <= 12."
  },
  {
   "id": 121,
   "type": "timeseries",
   "title": "Live data files",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 72
   },
   "targets": [
    {
     "expr": "max by (table) (loglake_table_live_data_files{namespace=~\"$namespace\"})",
     "legendFormat": "{{table}}",
     "refId": "A"
    }
   ]
  },
  {
   "id": 122,
   "type": "row",
   "title": "Query",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 80
   },
   "collapsed": false
  },
  {
   "id": 123,
   "type": "timeseries",
   "title": "Request rate by endpoint+status",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 0,
    "y": 81
   },
   "targets": [
    {
     "expr": "sum by (endpoint, status) (rate(loglake_query_requests_total{namespace=~\"$namespace\"}[1m]))",
     "legendFormat": "{{endpoint}} {{status}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "rps"
    }
   }
  },
  {
   "id": 124,
   "type": "timeseries",
   "title": "Request latency by endpoint (p50 / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 6,
    "y": 81
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.50, sum by (le, endpoint) (rate(loglake_query_request_duration_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p50 {{endpoint}}",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le, endpoint) (rate(loglake_query_request_duration_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99 {{endpoint}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s"
    }
   }
  },
  {
   "id": 125,
   "type": "timeseries",
   "title": "Memory pool reserved / limit",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 12,
    "y": 81
   },
   "targets": [
    {
     "expr": "max(loglake_query_memory_pool_reserved_bytes{namespace=~\"$namespace\"})",
     "legendFormat": "reserved",
     "refId": "A"
    },
    {
     "expr": "max(loglake_query_memory_pool_bytes{namespace=~\"$namespace\"})",
     "legendFormat": "limit",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "bytes"
    }
   },
   "description": "Every memory-related outage this project has had lived in the query pod. A limit of 0 means the pool is unbounded, which is the defect state."
  },
  {
   "id": 126,
   "type": "timeseries",
   "title": "Exec pool in-flight / abandoned",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 18,
    "y": 81
   },
   "targets": [
    {
     "expr": "sum(loglake_query_exec_pool_in_flight{namespace=~\"$namespace\"})",
     "legendFormat": "in flight",
     "refId": "A"
    },
    {
     "expr": "sum(increase(loglake_query_exec_pool_abandoned_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "abandoned",
     "refId": "B"
    }
   ],
   "description": "Accumulated abandonments preceded the degradation that only a restart cured. In-flight that never returns to zero is the leak shape."
  },
  {
   "id": 144,
   "type": "timeseries",
   "title": "Breaker trips by kind",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 89
   },
   "targets": [
    {
     "expr": "sum by (breaker, priority) (increase(loglake_query_breaker_trips_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{breaker}} {{priority}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "decimals": 0,
     "min": 0
    }
   },
   "description": "Breaker trips in the last five minutes, split by breaker kind and request priority. pool_exhausted means the query memory pool or spill cap refused work; admission and the remaining kinds identify the governing limit instead. The jaeger_* kinds (jaeger_trace_limit, jaeger_span_rows, jaeger_render_bytes, jaeger_name_rows) are the Jaeger read API's own render ceilings, derived from that pod's admission reservation: a request they refuse FITS the pod, so the fix is a narrower request or a larger admission budget, not memory pressure. Nothing filters this panel on breaker, so a kind added later appears here on its own. A batch timeout also appears as a timeout outcome in the separate batch-job panel; the two series describe the same run from breaker and job-lifecycle perspectives and must not be added."
  },
  {
   "id": 150,
   "type": "timeseries",
   "title": "Distributed-query shard pins",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 89
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_query_shard_pin_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{outcome}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Shard snapshot-pin outcomes per second. A miss refuses the distributed query with 503 rather than mixing snapshots. Sustained misses fire LoglakeQueryShardPinUnresolved; raise compactor.snapshotExpire.retainLast or lower LOGLAKE_ICEBERG_METADATA_CACHE_TTL_SECS."
  },
  {
   "id": 148,
   "type": "timeseries",
   "title": "Incomplete scan attribution rate",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 97
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_query_scan_attribution_incomplete_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "incomplete responses",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Rate of responses whose scan attribution was incomplete because at least one scan partition was still unwinding after the two-second settle deadline. Correlate with stats.scan.unsettled_partitions and the settle p99."
  },
  {
   "id": 149,
   "type": "timeseries",
   "title": "Scan settle p99 latency",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 97
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_query_scan_settle_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s",
     "min": 0
    }
   },
   "description": "Fleet-wide p99 time spent waiting for scan-partition pumps to finish after the root stream ends. The wait is bounded at two seconds; deadline hits increment the adjacent incomplete-attribution counter."
  },
  {
   "id": 153,
   "type": "timeseries",
   "title": "Batch job cancellation across replicas",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 105
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_query_jobs_cancel_propagated_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "aborted by a peer's cancellation",
     "refId": "A"
    },
    {
     "expr": "sum by (attempted, actual, cause) (rate(loglake_query_job_terminal_conflict_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "lifecycle conflict: {{attempted}} / {{actual}} ({{cause}})",
     "refId": "B"
    },
    {
     "expr": "sum by (outcome) (rate(loglake_query_jobs_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "job outcome: {{outcome}}",
     "refId": "C"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "The two halves of cancelling a batch job in a tier that shares one job store. The abort handle that releases a job's admission reservation and cancels its storage scans is process-local, so a DELETE served by a replica that is not executing the job can only persist `cancelled`; the executor picks that up on its own poll (--jobs-cancel-poll-secs, default 2s) and the propagated-abort series counts it. A healthy multi-replica tier shows that series tracking client cancellations; it sitting at zero while jobs are being cancelled means either every DELETE happened to land on the executor or the poll is not running. The lifecycle-conflict series names attempted and observed states with a bounded cause: attempted=running means the store refused the start and the query was never polled; succeeded, failed or timeout mean a completion was refused. Cancellation and gone are expected races, recovery pages for either a prevented start or discarded completion, write_deferred and write_abandoned are terminal writes that never landed (the panel to the right), and result_too_large is an applied failed completion. The job-outcome series reports only terminal states the executor actually INSTALLED, so an aborted or refused job appears in neither series; an oversized body appears as failed, never succeeded. The four cause=recovery series and the three cause=write_abandoned ones are pre-registered because alerts read them; other causes appear on first occurrence. scripts/check-chart.py holds these metric names to the source, so a rename fails CI instead of blanking the chart."
  },
  {
   "id": 154,
   "type": "timeseries",
   "title": "Batch jobs finished but not persisted",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 105
   },
   "targets": [
    {
     "expr": "sum(loglake_query_jobs_unreconciled{namespace=~\"$namespace\"})",
     "legendFormat": "parked, awaiting reconciliation",
     "refId": "A"
    },
    {
     "expr": "sum by (computed, outcome) (rate(loglake_query_jobs_reconciled_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "reconciled: {{computed}} -> {{outcome}}",
     "refId": "B"
    },
    {
     "expr": "sum(rate(loglake_query_jobs_unreconciled_dropped_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "refused, bookkeeping full (pages)",
     "refId": "C"
    },
    {
     "expr": "sum(rate(loglake_query_job_terminal_conflict_total{namespace=~\"$namespace\",cause=\"write_abandoned\"}[5m]))",
     "legendFormat": "stranded: write abandoned (pages)",
     "refId": "D"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "min": 0
    }
   },
   "description": "Batch runs whose execution ENDED without a terminal state being installed, because the job store was unreachable for every attempt the run could afford. The executor keeps a bounded, body-free note of those ids and retries the same conditional write every 5s: the parked gauge is that backlog and the reconciled rate is it draining, so a store outage shows as the gauge rising and then falling to zero. LoglakeBatchReconciliationBacklogStalled warns when a pod's parked gauge stays nonzero for 15 seconds; the verified Postgres recovery drained a 3/4-per-pod peak within 13.2 seconds. The bottom two series are the failure of that mechanism and both fire LoglakeBatchRowStrandedNonTerminal: bookkeeping full at 1024 ids per replica, and a run reporting that its write could not even be tracked. They move together by construction, and either one means a client polls `running` until this pod exits and lease-expiry recovery on a peer condemns the row. Note the mixed units: the parked series is a count of ids, the other three are per-second rates."
  },
  {
   "id": 128,
   "type": "row",
   "title": "Fast paths (healthy is hits, footer_only and a high hit ratio)",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 113
   },
   "collapsed": false
  },
  {
   "id": 129,
   "type": "timeseries",
   "title": "Side-aggregate cache by outcome",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 0,
    "y": 114
   },
   "targets": [
    {
     "expr": "sum by (result) (rate(loglake_query_side_aggs_cache_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{result}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops"
    }
   },
   "description": "hit = pinned aggregate served; miss = nothing pinned; stale = pinned for an older snapshot; uncovered = pinned, but not for the component asked about. The 2026-09-01 regression was 0 hits / 168 misses with stale ABSENT — the pin was never taken — and cost 11-242x per query. It sat there for two months because no panel showed this."
  },
  {
   "id": 130,
   "type": "timeseries",
   "title": "Tier-2 group-count calls by outcome",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 6,
    "y": 114
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_group_count_tier2_calls_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{outcome}}",
     "refId": "A"
    },
    {
     "expr": "(sum(rate(loglake_group_count_tier2_files_total{namespace=~\"$namespace\", outcome=~\"raw_page|boundary_scan\"}[5m])) or vector(0)) / sum(rate(loglake_group_count_tier2_files_total{namespace=~\"$namespace\", outcome=~\"footer|raw_page|boundary_scan\"}[5m]))",
     "legendFormat": "fallback share",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops"
    },
    "overrides": [
     {
      "matcher": {
       "id": "byFrameRefID",
       "options": "B"
      },
      "properties": [
       {
        "id": "unit",
        "value": "percentunit"
       },
       {
        "id": "custom.axisPlacement",
        "value": "right"
       },
       {
        "id": "min",
        "value": 0
       },
       {
        "id": "max",
        "value": 1
       }
      ]
     }
    ]
   },
   "description": "Tier 2 reports rows_scanned: 0 whether it read one footer per file (µs) or decoded raw pages per file (orders of magnitude slower); this is the only place the two differ. The 5-minute fallback share (right axis) is (raw_page + boundary_scan) / (footer + raw_page + boundary_scan), matching rollup_report.py: HEALTHY below 1%, MIXED from 1% to below 50%, and DEGENERATE at 50% or above."
  },
  {
   "id": 131,
   "type": "timeseries",
   "title": "Tier-2 files per call (p50 / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 12,
    "y": 114
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.50, sum by (le) (rate(loglake_group_count_tier2_files_per_call_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p50",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_group_count_tier2_files_per_call_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    }
   },
   "description": "Tier 2 walks the manifest and touches a footer per LIVE FILE on every call, so its cost tracks this number and the Layout row's live-file count. Rising with a flat query mix means compaction is not keeping the file count down."
  },
  {
   "id": 132,
   "type": "timeseries",
   "title": "File-list cache hit ratio",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 18,
    "y": 114
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_file_list_cache_hits_total{namespace=~\"$namespace\"}[5m])) / (sum(rate(loglake_file_list_cache_hits_total{namespace=~\"$namespace\"}[5m])) + sum(rate(loglake_file_list_cache_misses_total{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "hit ratio",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "percentunit",
     "min": 0,
     "max": 1
    }
   },
   "description": "Share of scans served from the snapshot-keyed live-file list instead of a fresh manifest walk. It invalidates on commit, so a ratio that falls with a steady query rate means the commit cadence outran the queries between them. Empty when no scan ran in the window."
  },
  {
   "id": 141,
   "type": "timeseries",
   "title": "Group-count deltas folded per read (p50 / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 122
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.50, sum by (le) (rate(loglake_group_count_deltas_folded_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p50",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le) (rate(loglake_group_count_deltas_folded_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "decimals": 0,
     "min": 0
    }
   },
   "description": "Per-commit group-count deltas still outstanding, folded on top of the wide aggregate by each Tier-1 read. The compactor's fold absorbs them into the wide file, so a healthy fleet sits near zero; a rising p99 means that fold is behind or stopped and every read is re-folding the backlog. Quantiles are fleet-wide over the last 5m of Tier-1 reads. The histogram has a 0 bucket, so a healthy fleet reads exactly 0 rather than the bottom of the first bucket; a window with no Tier-1 read shows a gap, not zero."
  },
  {
   "id": 142,
   "type": "timeseries",
   "title": "Group-count columns demoted to sketches",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 122
   },
   "targets": [
    {
     "expr": "sum by (column) (increase(loglake_group_count_column_demoted_total{namespace=~\"$namespace\"}[15m]))",
     "legendFormat": "{{column}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "decimals": 0,
     "min": 0
    }
   },
   "description": "Times a fold found a column's exact group-count aggregate above LOGLAKE_TABLE_GROUP_COUNT_CARDINALITY and converted it to a bounded sketch, by column. The first crossing is the one that matters: from then on GROUP BY on that column is approximate, and only the response's approximation field says so. Raise the cap above the column's distinct count to keep it exact. Should sit at zero."
  },
  {
   "id": 151,
   "type": "timeseries",
   "title": "Table-metadata cache fences by action",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 130
   },
   "targets": [
    {
     "expr": "sum by (pod, action) (rate(loglake_iceberg_table_cache_fenced_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{pod}} {{action}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Reloads of a table's Iceberg metadata that lost their publication race (#1561), per pod. superseded (served the entry that beat it) and reload (re-loaded because the entry was invalidated) are healthy contention under a commit storm. unpublished is not: those reloads ran out of attempts, so the table's metadata cache stays empty and every read pays a full load_table. All three arms are pre-registered at 0, so a flat zero line is a working cache, not a missing series; a sustained unpublished rate fires LoglakeTableCacheUnpublished."
  },
  {
   "id": 152,
   "type": "timeseries",
   "title": "SQL result cache by outcome",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 130
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_query_sql_result_cache_requests_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{outcome}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Every decision the snapshot-keyed SQL result cache takes. hit/miss/insert/wait are the working cache. Not only /api/v1/sql since #2268: the two Jaeger name-list routes (services, operations) share this store and this counter rather than owning a second one, so a Jaeger UI polling in the background shows up here as hits it did not used to produce. Their misses carry none of the skip_ arms below — the routes serve two fixed SELECT DISTINCTs over the committed snapshot, so there is no statement to classify and no WAL union to prove empty. The skip_ arms say why a request was refused one: skip_buffer_* is the WAL proof (rows in flight, a refused decode, a failed load, a witnessed segment set that moved) and is expected while a backlog drains; skip_time_dependent is a query calling a registered STABLE or VOLATILE function (now(), random()) and is ordinary dashboard traffic; skip_unclassifiable is a function name the planner's registry does not hold — the conservative classifier costing this deployment its cache on queries that may plan and succeed anyway; skip_unparseable is a statement the cache classifier's own parser rejected and should sit at ~zero (#1481). Uncacheable SHAPES — bare unordered scans — are not counted here at all. No arm is pre-registered at 0 (no alert reads them), so a missing series here means that outcome has not happened on this pod — not a broken panel: scripts/check-chart.py holds this metric name to the source, so a rename fails CI instead of blanking the chart."
  },
  {
   "id": 159,
   "type": "timeseries",
   "title": "Text-index startup by stage (p50 / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 0,
    "y": 138
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.50, sum by (le, stage) (rate(loglake_iceberg_text_index_startup_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p50 {{stage}}",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le, stage) (rate(loglake_iceberg_text_index_startup_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99 {{stage}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s",
     "min": 0
    }
   },
   "description": "What a text query spends before a planned indexed file produces its first batch, split into the four stages that make the number actionable: permit_wait is the global index-load semaphore (LOGLAKE_INDEX_LOAD_CONCURRENCY, 4 by default) making a plan's files queue; blob_fetch is the Puffin blob read from object storage; decode is InvertedIndex::from_bytes, which costs about 30 ns per indexed row and is paid only when the parsed-index cache missed; selection is the postings lookup plus the row-selection runs, paid on every file the index prunes. A first-batch regression is one of those four, and run #73 could not say which from the round's artifacts. Read against the cache panel beside it: decode's sample COUNT is the number of cold loads, selection's is the number of index-pruned files, so decode climbing toward selection means the cache is not holding the plan's indexes. The stage is a grouping, not a filter, so a stage added later appears here without editing the dashboard."
  },
  {
   "id": 160,
   "type": "timeseries",
   "title": "Parsed text-index cache outcomes / evictions",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 8,
    "y": 138
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_iceberg_parsed_index_cache_lookups_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "lookup {{outcome}}",
     "refId": "A"
    },
    {
     "expr": "sum by (reason) (rate(loglake_iceberg_parsed_index_cache_evictions_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "dropped {{reason}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Whether the plan's parsed indexes stay warm. One lookup outcome per file a text query acquires an index for: hit means the parsed form was reused, miss means this query deserialized the blob. Both arms are pre-registered at 0 on the query server, so a tier serving no text query charts a flat zero rather than No data. The dropped arms are why a miss happened and they are the reading a hit ratio cannot give: byte_bound is the working set over LOGLAKE_PARSED_INDEX_CACHE_MAX_BYTES (1 GiB by default, about 40 bytes per indexed row, so five compacted 7M-row files need about 1.5 GB), entry_bound is the shared LOGLAKE_PUFFIN_BLOB_CACHE_MAX_ENTRIES count, and oversized is one index alone exceeding the whole budget, which leaves that file decoding on every query. Misses with no drops are first reads; misses beside a steady byte_bound rate are thrash, which is what run #78's keyword_and_label plan was doing with 4.1 GB of parsed index against the default."
  },
  {
   "id": 161,
   "type": "timeseries",
   "title": "Parsed text-index cache resident / budget",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 16,
    "y": 138
   },
   "targets": [
    {
     "expr": "max(loglake_iceberg_parsed_index_cache_bytes{namespace=~\"$namespace\"})",
     "legendFormat": "resident",
     "refId": "A"
    },
    {
     "expr": "max(loglake_iceberg_parsed_index_cache_max_bytes{namespace=~\"$namespace\"})",
     "legendFormat": "budget",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "bytes",
     "min": 0
    }
   },
   "description": "The parsed working set against the bound that evicts it. Both are published where the bounds are enforced — on admission to the cache — so neither exists before the pod's first indexed text query and both hold their last value while no text query runs; read them with the eviction arms beside them, not on their own. Resident pinned at the budget with byte_bound drops beside it is the case for raising LOGLAKE_PARSED_INDEX_CACHE_MAX_BYTES (or the query pod's memory limit, which the resolved budget follows when it is set)."
  },
  {
   "id": 165,
   "type": "timeseries",
   "title": "Puffin blob cache fetches / hits / evictions",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 146
   },
   "targets": [
    {
     "expr": "sum(rate(loglake_iceberg_puffin_blob_fetches_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "fetch",
     "refId": "A"
    },
    {
     "expr": "sum by (outcome) (rate(loglake_iceberg_puffin_blob_cache_lookups_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "lookup {{outcome}}",
     "refId": "B"
    },
    {
     "expr": "sum by (reason) (rate(loglake_iceberg_puffin_blob_cache_evictions_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "dropped {{reason}}",
     "refId": "C"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Whether a re-decode also had to re-read the blob. fetch is the object-store read this cache exists to stop; lookup hit is a decode handed bytes the cache still held and lookup miss is one that had to fetch. Every arm is pre-registered at 0 on the query server, so a tier serving no text query charts zero rather than No data. Read it beside the parsed-index panel: a parsed miss is what brings a query here at all, so hits climbing with parsed misses is a working set past the parsed budget but inside the blob budget, which costs a decode and no read. A fetch rate that tracks the parsed miss rate is the #4182 shape — every execution re-reading every blob — and it was inferred from index-phase bytes and first_batch_ms for a whole round before these counters existed. fetch above miss is index reading that consulted no cache: a rebuild that bypasses it, or a pod with LOGLAKE_PUFFIN_BLOB_CACHE_MAX_BYTES or _MAX_ENTRIES at 0. dropped oversized is the other reading this panel owes an operator: a file whose index blob alone exceeds LOGLAKE_PUFFIN_BLOB_CACHE_MAX_BYTES is never admitted, so it re-reads on every decode while the cache holds it never \u2014 a pod one blob short of its plan's per-file index otherwise charts exactly what a cold cache and a disabled one chart. The other dropped arms name the eviction rule that chose each victim: redundant is a blob whose parsed twin is resident, which cannot be read until that twin goes, stale is one nothing read while the cache turned over four times (its file has left the plan), and fifo is the fallback for a cache holding nothing live and recent — a steady fifo rate beside a fetch rate is the pre-#4182 behaviour, which dropped a blob one step before the query that wanted it. On run #81's keyword_and_label plan that difference was 4.60 GB over 183 index-phase reads against 0.50 GB over 73."
  },
  {
   "id": 162,
   "type": "timeseries",
   "title": "Decoded-file cache populations (abandoned = decoded, then dropped)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 18,
    "x": 0,
    "y": 154
   },
   "targets": [
    {
     "expr": "sum by (outcome) (rate(loglake_query_scan_file_cache_requests_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{outcome}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops",
     "min": 0
    }
   },
   "description": "Decoded-file cache populations, by what happened to each one. The cache is experimental and OFF by default (LOGLAKE_QUERY_SCAN_FILE_CACHE_MAX_BYTES and _MAX_ENTRIES are 0 in the chart, the compose file and the operator), so a flat zero on every arm is the expected reading for most deployments; all eight arms are pre-registered on the query server, so zero charts as zero rather than No data. abandoned is the arm to read when it IS on (#4846): a population that decoded batches, buffered them and was dropped before the end-of-stream arm could insert them — a LIMIT satisfied from the first batches, or a cancelled query. It cannot be read off miss, which is charged when a partition OPENS its first task, once per partition: the 2026-09-15 50G rounds exported 448 miss and no insert at 8 GiB, and #4494 had to establish from four ABSENT series that those were opens rather than populations that failed to stick. abandoned at the miss rate with insert at zero is that same shape, stated directly: the budget is subtracted from the query memory pool and nothing is ever kept. insert against abandoned is therefore the readout of whether the cache is earning its bytes on this workload. The other arms: bypass is a scan with a raw-text or promoted-column predicate, which reads through the pruning reader by design and never populates; skip_oversized is one file's decoded batches crossing a quarter of the budget; insert_skipped_contended is the deliberate degrade when a finished population loses the cache lock — one skip costs one future miss, and the panel beside this one shows its share of completed insert attempts per query pod; evict is the LRU bound biting. The arms do not partition miss: miss is charged before the populate stream is built, and hit and bypass never build one."
  },
  {
   "id": 172,
   "type": "timeseries",
   "title": "Decoded-file cache contended-insert share",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 6,
    "x": 18,
    "y": 154
   },
   "targets": [
    {
     "expr": "((sum by (namespace, pod) (rate(loglake_query_scan_file_cache_requests_total{namespace=~\"$namespace\",outcome=\"insert_skipped_contended\"}[5m])) or sum by (namespace, pod) (rate(loglake_query_scan_file_cache_requests_total{namespace=~\"$namespace\",outcome=~\"insert|insert_skipped_contended\"}[5m])) * 0) / (sum by (namespace, pod) (rate(loglake_query_scan_file_cache_requests_total{namespace=~\"$namespace\",outcome=~\"insert|insert_skipped_contended\"}[5m])) > 0)) or sum by (namespace, pod) (rate(loglake_query_scan_file_cache_requests_total{namespace=~\"$namespace\",outcome=~\"insert|insert_skipped_contended\"}[5m])) * 0",
     "legendFormat": "{{namespace}} / {{pod}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "percentunit",
     "min": 0,
     "max": 1
    }
   },
   "description": "The share of completed decoded-file cache insert attempts that lost the cache's try_lock, per query pod: insert_skipped_contended / (insert + insert_skipped_contended). A skip is safe because another reader can rebuild the entry, but it costs that future miss and decode. A sustained high share means contention is preventing the cache from filling; no alert is set until #3053 supplies a representative threshold and sustain window. The two outcome series are pre-registered at zero. An idle pod is rendered as 0%; confirm that the raw insert arms beside it are active before interpreting the percentage."
  },
  {
   "id": 166,
   "type": "timeseries",
   "title": "Footer text-index checksum refusals",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 162
   },
   "targets": [
    {
     "expr": "sum by (reason) (increase(loglake_index_footer_checksum_refused_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{reason}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "min": 0
    }
   },
   "description": "Footer-KV inverted indexes refused before pruning because their sibling CRC-32 is malformed or disagrees with the stored blob. The reader falls back to Puffin when present and otherwise scans the file exactly, so a rising rate means correctness was preserved at the cost of scan work. Both reasons are pre-registered at zero on query pods."
  },
  {
   "id": 167,
   "type": "timeseries",
   "title": "Index sidecars written / refused by reason",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 170
   },
   "targets": [
    {
     "expr": "sum by (outcome, reason) (increase(loglake_iceberg_segmented_index_writes_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "{{outcome}} {{reason}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "min": 0
    },
    "overrides": []
   },
   "description": "What the sidecar writer decided at the close of a compaction rewrite. written is charged once per (output file, indexed column); a refusal is charged once per output file, because the writer abandons that file's whole sidecar set at the first column it cannot stand behind. written is a segmented (seg2) sidecar handed to the registration; the three refused reasons are the ways a sidecar would have described a file layout the file does not have, and each one leaves that file on the scan path with no index: column is a row group whose text column could not be indexed, file_rows is a sidecar whose total rows disagree with the file's, and row_domain is a sidecar whose per-group rows disagree with the footer's row groups. A refusal is the safe outcome (the reader scans exactly, as it does for an unindexed file), so nothing here is a correctness signal — it is the one place a compactor writing files without indexes is visible. Every arm is pre-registered at 0 on the compactor, so a pod with the writer switched off charts flat zeros rather than No data: the writer is opt-in (LOGLAKE_SEGMENTED_INDEX_WRITES=1) and an all-zero panel is the correct reading of a default install. Read it against the v1 rebuild panels below: a file the writer refused is a file the post-rewrite v1 rebuild has to decode whole, if that is on at all."
  },
  {
   "id": 168,
   "type": "timeseries",
   "title": "Index sidecar bytes / per-group index bytes (p50 / p99 per pod)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 170
   },
   "targets": [
    {
     "expr": "loglake_iceberg_segmented_index_written_bytes{namespace=~\"$namespace\", quantile=\"0.5\"}",
     "legendFormat": "p50 sidecar {{pod}}",
     "refId": "A"
    },
    {
     "expr": "loglake_iceberg_segmented_index_written_bytes{namespace=~\"$namespace\", quantile=\"0.99\"}",
     "legendFormat": "p99 sidecar {{pod}}",
     "refId": "B"
    },
    {
     "expr": "loglake_iceberg_segmented_index_group_index_bytes{namespace=~\"$namespace\", quantile=\"0.5\"}",
     "legendFormat": "p50 group index {{pod}}",
     "refId": "C"
    },
    {
     "expr": "loglake_iceberg_segmented_index_group_index_bytes{namespace=~\"$namespace\", quantile=\"0.99\"}",
     "legendFormat": "p99 group index {{pod}}",
     "refId": "D"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "bytes",
     "min": 0
    },
    "overrides": []
   },
   "description": "What a seg2 sidecar costs, in the two units that answer different questions. sidecar is one finished blob's encoded size — the Puffin bytes the rewrite commits and a later text query fetches. group index is the heap one row group's postings and dictionary occupy while the writer builds them, recorded per row group and dropped at the end of that group: it is the bound the streaming design rests on, not the writer's peak live heap, and a series that grows with file size would mean the build had become file-proportional. Both are exported as summaries, so these are each pod's own quantiles over the exporter's rolling window and are charted per pod rather than summed or averaged — a mean of percentiles is not a percentile, and one compactor's outlier rewrite is the reading. Neither series exists before that pod's first sidecar: quantiles cannot be pre-registered, and the writer is opt-in, so No data here means no seg2 sidecar has closed on this pod."
  },
  {
   "id": 169,
   "type": "timeseries",
   "title": "v1 index rebuild: files and source bytes",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 178
   },
   "targets": [
    {
     "expr": "sum by (table) (increase(loglake_index_rebuild_files_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "files/h {{table}}",
     "refId": "A"
    },
    {
     "expr": "sum by (table) (increase(loglake_index_rebuild_bytes_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "source bytes/h {{table}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "min": 0
    },
    "overrides": [
     {
      "matcher": {
       "id": "byFrameRefID",
       "options": "B"
      },
      "properties": [
       {
        "id": "unit",
        "value": "bytes"
       },
       {
        "id": "custom.axisPlacement",
        "value": "right"
       }
      ]
     }
    ]
   },
   "description": "The opt-in post-rewrite index rebuild (#4162), which reads a committed data file back whole and builds a v1 inverted index for every column the rewrite left without one. files is how many data files one pass rebuilt; source bytes (right axis) is those files' own size, which is the Parquet the pass re-read and decoded — not the index it produced. Both are charged once per pass and only after the registration commits, so a pass that rebuilt nothing, or whose registration was deferred, adds nothing here; the \"Index registrations deferred\" panel is where that second case shows. Neither series is pre-registered — both carry the tenant and table, known only at the increment — so a table that has never been rebuilt is absent rather than zero, and No data on the whole panel means no rebuild has committed on any pod. Rising files with the sidecar panel above flat is the compactor paying for whole-file decodes the seg2 writer would have avoided."
  },
  {
   "id": 170,
   "type": "timeseries",
   "title": "v1 index rebuild duration (p50 / p99)",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 178
   },
   "targets": [
    {
     "expr": "histogram_quantile(0.50, sum by (le, table) (rate(loglake_index_rebuild_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p50 {{table}}",
     "refId": "A"
    },
    {
     "expr": "histogram_quantile(0.99, sum by (le, table) (rate(loglake_index_rebuild_seconds_bucket{namespace=~\"$namespace\"}[5m])))",
     "legendFormat": "p99 {{table}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s",
     "min": 0
    },
    "overrides": []
   },
   "description": "How long one rebuild pass took, end to end: the footer probe over every candidate file, the whole-file decode of the ones that needed rebuilding, and the Puffin registration. A pass that rebuilt nothing, or whose registration was deferred, records nothing here at all. A classic histogram (loglake-core buckets every _seconds name), so these are quantiles over the fleet's aggregated buckets rather than one pod's — unlike the byte panel beside it. The pass runs inside a compaction cycle and holds it, so a p99 here that approaches the cycle interval is a compactor whose rebuild is the cycle. Read the sample count against the files panel to its left: the same pass can rebuild one file or many, and a p99 that climbs with a flat file rate is per-file decode cost, not more work."
  },
  {
   "id": 137,
   "type": "row",
   "title": "Cache warmer",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 186
   },
   "collapsed": false
  },
  {
   "id": 138,
   "type": "timeseries",
   "title": "Age since last completed warm cycle",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 0,
    "y": 187
   },
   "targets": [
    {
     "expr": "time() - loglake_query_warm_last_completed_seconds{namespace=~\"$namespace\"}",
     "legendFormat": "{{pod}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "s",
     "min": 0,
     "custom": {
      "thresholdsStyle": {
       "mode": "line"
      }
     },
     "thresholds": {
      "mode": "absolute",
      "steps": [
       {
        "color": "green",
        "value": null
       },
       {
        "color": "red",
        "value": 90
       }
      ]
     }
    }
   },
   "description": "Seconds since each query pod last completed a cache-warm cycle. The red line is three times the chart's default 30s interval, matching LoglakeQueryWarmCycleStalled; keep it aligned if the deployment overrides the interval. A new pod has no series until its first cycle completes, so consult the in-progress panel beside it during startup."
  },
  {
   "id": 139,
   "type": "timeseries",
   "title": "Warm cycle in progress",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 8,
    "y": 187
   },
   "targets": [
    {
     "expr": "loglake_query_warm_cycle_in_progress{namespace=~\"$namespace\"}",
     "legendFormat": "{{pod}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short",
     "decimals": 0,
     "min": 0,
     "max": 1
    }
   },
   "description": "One while a query pod is warming caches, zero between cycles. A pod pinned at one is wedged mid-cycle; a stale last-completed age while pinned at zero means the periodic warm loop may have stopped."
  },
  {
   "id": 140,
   "type": "timeseries",
   "title": "Warm-cycle failure rate",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 8,
    "x": 16,
    "y": 187
   },
   "targets": [
    {
     "expr": "sum by (pod) (rate(loglake_query_warm_cycles_abandoned_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{pod}} abandoned",
     "refId": "A"
    },
    {
     "expr": "sum by (pod) (rate(loglake_query_warm_cycle_timeouts_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{pod}} cycle timeout",
     "refId": "B"
    },
    {
     "expr": "sum by (pod) (rate(loglake_query_warm_probe_timeouts_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{pod}} probe timeout",
     "refId": "C"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "ops"
    }
   },
   "description": "Per-pod rates over five minutes. Every cycle timeout is also an abandonment; abandonment without a cycle timeout is cancellation or panic. Probe timeouts are best-effort failures within a cycle and do not by themselves abandon it."
  },
  {
   "id": 145,
   "type": "row",
   "title": "Durable reclaim proof",
   "gridPos": {
    "h": 1,
    "w": 24,
    "x": 0,
    "y": 195
   },
   "collapsed": false
  },
  {
   "id": 146,
   "type": "timeseries",
   "title": "Consumed-proof size",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 196
   },
   "targets": [
    {
     "expr": "sum by (table) (loglake_consumed_proof_entries{namespace=~\"$namespace\"})",
     "legendFormat": "entries {{table}}",
     "refId": "A"
    },
    {
     "expr": "sum by (table) (loglake_consumed_proof_bytes{namespace=~\"$namespace\"})",
     "legendFormat": "bytes {{table}}",
     "refId": "B"
    }
   ],
   "description": "Unacknowledged committed segment IDs and their encoded table-property bytes. Both should return to a small steady state after catalog marks are acknowledged."
  },
  {
   "id": 147,
   "type": "timeseries",
   "title": "Consumed-proof watermark lag / cap refusals",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 12,
    "x": 12,
    "y": 196
   },
   "targets": [
    {
     "expr": "max by (table) (loglake_consumed_proof_watermark_lag_seconds{namespace=~\"$namespace\"})",
     "legendFormat": "last successful update {{table}}",
     "refId": "A"
    },
    {
     "expr": "max by (table) (loglake_consumed_proof_current_watermark_lag_seconds{namespace=~\"$namespace\"})",
     "legendFormat": "current attempt {{table}}",
     "refId": "C"
    },
    {
     "expr": "sum(increase(loglake_consumed_proof_cap_refusals_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "cap refusals",
     "refId": "B"
    }
   ],
   "description": "Current-attempt lag keeps moving during refused commits; last-successful lag shows when the property stopped updating. Exact terminal IDs compact independently of a pinned time watermark. Any cap refusal fails writes closed and fires LoglakeConsumedProofAtCapacity."
  },
  {
   "id": 157,
   "type": "timeseries",
   "title": "Consumed-proof watermark refusals",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 204
   },
   "targets": [
    {
     "expr": "sum by (reason, index) (increase(loglake_compactor_proof_watermark_skipped_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "refusals/h {{reason}} {{index}}",
     "refId": "A"
    },
    {
     "expr": "sum by (reason, index) (loglake_compactor_proof_watermark_skipped_total{namespace=~\"$namespace\"})",
     "legendFormat": "refusals (cumulative) {{reason}} {{index}}",
     "refId": "B"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Counts refused consumed-proof boundaries by index and reason. A refusal leaves that boundary uncompacted; it does not prove that all proof compaction for the index has stopped. The next terminal claim can establish a verified boundary and restore progress. The cumulative arm preserves a first event whose series did not exist at the preceding Prometheus scrape."
  },
  {
   "id": 158,
   "type": "timeseries",
   "title": "Query audit whole-row drops",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 212
   },
   "targets": [
    {
     "expr": "sum by (reason) (increase(loglake_query_audit_dropped_total{namespace=~\"$namespace\"}[5m]))",
     "legendFormat": "{{reason}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Whole audit rows lost without changing query responses. Oversized is a single row above the byte ceiling; row_limit and byte_limit are retained-budget saturation; channel_full is a transient sender/worker race; worker_shutdown means no audit receiver remains. Those five are refusals at submit. append means a storage append returned an error and its whole batch was dropped without retry, counted once per row. append_deadline means an append outlived its service deadline and its batch was abandoned, counted once per row; its commit may or may not have landed."
  },
  {
   "id": 164,
   "type": "timeseries",
   "title": "Index registrations deferred",
   "datasource": {
    "type": "prometheus",
    "uid": "${datasource}"
   },
   "gridPos": {
    "h": 8,
    "w": 24,
    "x": 0,
    "y": 220
   },
   "targets": [
    {
     "expr": "sum by (reason) (increase(loglake_index_registration_deferred_total{namespace=~\"$namespace\"}[1h]))",
     "legendFormat": "{{reason}}",
     "refId": "A"
    }
   ],
   "fieldConfig": {
    "defaults": {
     "unit": "short"
    },
    "overrides": []
   },
   "description": "Puffin index registrations refused because the target snapshot already carries a statistics file. The existing file stays discoverable; the warning log names data files left on the scan path until a later snapshot."
  }
 ]
}
