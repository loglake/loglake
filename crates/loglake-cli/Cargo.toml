[package]
name = "loglake-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "loglake CLI: smoke-test ingest and query for the loglake storage layer."

[[bin]]
name = "loglake"
path = "src/main.rs"

[features]
# Passthrough for a profiling build (`deploy/Dockerfile` with `PROFILING=1`).
# Two halves, and BOTH have to be here rather than in loglake-core: the routes
# come from loglake-core, but `--enable-prof` is a property of the jemalloc that
# *this* crate's `#[global_allocator]` links, so loglake-core cannot turn it on.
# Without the second half the heap route answers 412.
profiling = ["loglake-core/profiling", "tikv-jemallocator/profiling"]

[dependencies]
reqwest = { workspace = true }
rustyline = { workspace = true }
loglake-core = { workspace = true }
loglake-storage = { workspace = true }
loglake-wal = { workspace = true }
loglake-ingest = { workspace = true }
loglake-compactor = { workspace = true }

anyhow = { workspace = true }
metrics = { workspace = true }
clap = { workspace = true }
chrono = { workspace = true }
datafusion = { workspace = true }
iceberg = { workspace = true }
object_store = { workspace = true }
opendal = { workspace = true }
reqsign-core = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
url = "2"
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
async-trait = { workspace = true }
uuid = { workspace = true }
tikv-jemallocator = "0.7.0"

[dev-dependencies]
arrow-array = { workspace = true }
axum = { workspace = true }
libc = "0.2"
metrics-util = "0.20"
serde_json = { workspace = true }
tempfile = "3"
tokio = { workspace = true }
tower = { workspace = true }

[lints]
workspace = true
