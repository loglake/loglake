/target
**/*.rs.bk
Cargo.lock.bak

# Local data dirs used by smoke tests
/data
/.loglake

# Evidence a round writes for the manager to collect (scripts/kind-round.sh)
# scripts/release-validation/README.md:114 commits reviewed summaries here; other round output under results/ stays ignored.
/results/*
!/results/release-validation/

# Local reference clones for study (not part of loglake)
/compare

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
__pycache__/
*.py[cod]

# Terraform working state
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!example.tfvars
crash.log
crash.*.log
.aws-env
