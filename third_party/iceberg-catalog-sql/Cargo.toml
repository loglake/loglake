# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# The original Cargo.toml is not vendored: the pristine upstream package,
# Cargo.toml.orig included, is the crates.io copy in the cargo registry.
# loglake's divergences from it are git history plus third_party/README.md.

[package]
edition = "2024"
rust-version = "1.92"
name = "iceberg-catalog-sql"
version = "0.9.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Apache Iceberg Rust Sql Catalog"
homepage = "https://rust.iceberg.apache.org/"
readme = false
keywords = [
    "iceberg",
    "sql",
    "catalog",
]
categories = ["database"]
license = "Apache-2.0"
repository = "https://github.com/apache/iceberg-rust"
resolver = "2"

[lib]
name = "iceberg_catalog_sql"
path = "src/lib.rs"

[dependencies.async-trait]
version = "0.1.89"

[dependencies.iceberg]
version = "0.9.1"

[dependencies.metrics]
version = "0.24"

[dependencies.sqlx]
version = "0.8.1"
features = ["any"]
default-features = false

[dependencies.strum]
version = "0.27.2"

[dev-dependencies.itertools]
version = "0.13"

[dev-dependencies.regex]
version = "1.11.3"

[dev-dependencies.sqlx]
version = "0.8.1"
features = [
    "tls-rustls",
    "runtime-tokio",
    "any",
    "sqlite",
    "migrate",
]
default-features = false

[dev-dependencies.tempfile]
version = "3.18"

# loglake: test-only tokio features. `macros` for the `#[tokio::test]` and
# `#[tokio::main]` attributes -- upstream's entry names neither and gets them
# only when something else in the graph (sqlx's `runtime-tokio`) turns them on
# -- and `rt-multi-thread` because `#[tokio::main]` defaults to that flavour,
# which the crate-root doctest needs. Without this entry that doctest does not
# compile standalone, the gap #2671 found in third_party/iceberg.
[dev-dependencies.tokio]
version = "1.47"
features = ["macros", "rt-multi-thread"]
default-features = false
